from django.apps import AppConfig


class GerenciaCalidadConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.gerencia_calidad'
    verbose_name = 'Gerencia de Calidad'
